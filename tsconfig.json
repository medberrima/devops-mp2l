{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,  // Allows JavaScript files alongside TypeScript
    "skipLibCheck": true,  // Skips type checking of declaration files to speed up the compilation
    "strict": true,  // Enables strict type-checking options
    "noEmit": true,  // Prevents emitting compiled JavaScript files, useful if you're using Babel
    "esModuleInterop": true,  // Ensures compatibility with CommonJS modules
    "module": "esnext",  // Use the latest module system
    "moduleResolution": "bundler",  // Resolves modules based on bundler's behavior (next.js)
    "resolveJsonModule": true,  // Allows importing JSON files as modules
    "isolatedModules": true,  // Ensures files are transpileable without relying on type information
    "jsx": "preserve",  // Keeps JSX as-is for later transformation by a build tool
    "incremental": true,  // Enables incremental compilation to speed up rebuilds
    "plugins": [
      {
        "name": "next"  // Ensures Next.js features are supported in the compiler
      }
    ],
    "paths": {
      "@/*": ["./src/*"]  // Allows imports using the '@' alias for the 'src' directory
    }
  },
  "include": [
    "next-env.d.ts",  // Includes the default Next.js types
    "**/*.ts",  // Includes all TypeScript files
    "**/*.tsx",  // Includes all TypeScript JSX files
    ".next/types/**/*.ts",  // Includes types generated by Next.js
    "src/app/page.js"  // Includes the JavaScript file in the 'src/app' directory
, "src/app/layout.jsx"  ],
  "exclude": [
    "node_modules"  // Excludes the 'node_modules' folder from being processed
  ]
}
